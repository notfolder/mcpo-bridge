{
  "mcpServers": {
    "powerpoint": {
      "command": "uvx",
      "args": [
        "--from",
        "office-powerpoint-mcp-server",
        "ppt_mcp_server"
      ],
      "env": {},
      "mode": "stateful",
      "idle_timeout": 3600,
      "max_processes_per_ip": 1,
      "session_persistence": true,
      "file_path_fields": ["file_path", "saved_path", "template_path"],
      "usage_guide": "\n🚨 **CRITICAL: このツールセットでPowerPointファイルを生成する時は、必ずツールを最後まで実行してください** 🚨\n\n## ⚠️ 絶対禁止事項（NEVER DO THIS）\n\n❌ python-pptxのコード例を提示すること\n❌ 「送ってください」などユーザーへの確認を求めること\n❌ ツールの説明や使い方の解説をすること\n❌ Pythonスクリプトの作成\n❌ ファイル作成の代替案の提示\n\n## ✅ 必須の動作（ALWAYS DO THIS）\n\n1. ユーザーが「PowerPointファイルを作成して」と言ったら:\n   - 即座に`create_presentation()`を呼ぶ\n   - レスポンスから`presentation_id`を取得\n   - **以降の全てのツール呼び出しでこの`presentation_id`を使用**\n   - スライドを追加（`add_slide(presentation_id=\"取得したID\")`など）\n   - **必ず`save_presentation(presentation_id=\"取得したID\")`を呼んでファイルを保存**\n   - レスポンスに含まれるダウンロードリンクをそのまま提示\n\n2. ワークフロー:\n   ```\n   result = create_presentation()\n   pres_id = result['presentation_id']  // 例: \"presentation_1\"\n   add_slide(presentation_id=pres_id, ...)\n   add_slide(presentation_id=pres_id, ...)\n   save_presentation(file_path=\"file.pptx\", presentation_id=pres_id)\n   ```\n\n3. **presentation_idの扱いが最重要**:\n   - `create_presentation()`のレスポンスに含まれる`presentation_id`を必ず保持\n   - 以降の全てのツール（`add_slide`, `save_presentation`等）で**必ず指定**\n   - `presentation_id=None`を渡すと必ず失敗します\n\n4. `save_presentation()`の実行は**絶対必須**です。これを省略すると:\n   - ファイルが生成されない\n   - ダウンロードリンクが出ない\n   - ユーザーは何も受け取れない\n\n## 📎 結果の提示方法\n\n`save_presentation()`のレスポンスに含まれる形式のダウンロードリンク:\n```\n📎 ダウンロード: [filename.pptx](http://...)\n```\nを**そのまま**ユーザーに提示してください。\n\n---\n**このツール自体は実行不可。ドキュメント用ダミーツールです。**\n"
    },
    "excel": {
      "command": "uvx",
      "args": [
        "excel-mcp-server",
        "stdio"
      ],
      "mode": "stateless",
      "usage_guide": "\n# Excel Tool Usage Guide\n\nExcelファイル生成ツールの使用方法。\n\n## 🚀 即座実行ポリシー\n\n**ユーザーの確認を待たずに即座にツールを実行してください。**\n\n## 📈 利用可能なツール\n\n以下のツールを直接使用してください：\n- `create_workbook`\n- `write_data_to_excel`\n- `format_range`\n\n### 禁止事項\n\n- ❌ openpyxlやpandasのコード例の提案\n- ❌ CSVエクスポートの代替案\n- ❌ ユーザーへの確認プロンプト\n\n---\n\n**注意**: このツール自体は実行できません。ドキュメント用のダミーツールです。\n"
    }
  }
}
